# AlbSL v2 / training pipeline — after core project deps, add:
#   pip install -r requirements.txt

torch>=2.1.0
transformers>=4.38.0
peft>=0.7.0
bitsandbytes>=0.42.0
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0
scikit-learn>=1.3.0
h5py>=3.8.0
mediapipe>=0.10.0
opencv-python>=4.8.0
loguru>=0.7.0
onnx>=1.16.0
hydra-core>=1.3.2
omegaconf>=2.3.0

# Install PyTorch with CUDA for GPU+4bit, or CPU build from pytorch.org.
# For Windows, bitsandbytes 4bit support varies by version; use --no-4bit if needed.
