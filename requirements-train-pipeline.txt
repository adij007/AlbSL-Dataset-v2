# Core stack for consolidate_data.py, train_albsl.py, albsl_app_v3.py
# Install: pip install -r requirements-train-pipeline.txt

# PyTorch: pick CPU or install from pytorch.org (CUDA) matching your GPU.
torch>=2.1.0
transformers>=4.38.0
peft>=0.7.0
bitsandbytes>=0.42.0; platform_system != "Windows" or os_name == "nt"
# bitsandbytes on Windows may be limited; train_albsl falls back to fp32 on CPU.

numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0
scikit-learn>=1.3.0
h5py>=3.8.0
mediapipe>=0.10.0
opencv-python>=4.8.0
loguru>=0.7.0
onnx>=1.16.0; extra == "export"
hydra-core>=1.3.2
omegaconf>=2.3.0

# Optional: onnxruntime for running exported ONNX
# onnxruntime>=1.17.0
